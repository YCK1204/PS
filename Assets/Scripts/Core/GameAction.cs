namespace PS.Core
{
    /// <summary>키를 바꿔 끼울 수 있는 동작. 순서가 설정 화면 표시 순서다.</summary>
    public enum GameAction
    {
        MoveLeft,
        MoveRight,
        Jump,
        Dash,
        Attack,
        Inventory,
    }
}
